# 项目协作与交付规范

## 总体要求

本项目的每次功能修改、修复、重构或配置变更，都必须完成以下闭环：

1. 了解当前代码、数据模型、部署配置和既有改动。
2. 实现修改，并检查新增逻辑对其他模块的影响。
3. 提交前进行完整的代码 Review。
4. 运行与改动范围匹配的测试、类型检查和构建验证。
5. 提交到 GitHub。
6. 部署到 Railway，并核验线上服务和关键业务链路。

除非用户明确要求只分析或只提供方案，否则不要在只完成本地修改后结束任务。

## 修改前检查

- 先检查 `git status`、当前分支、最近提交和未提交改动。
- 保留用户已有的修改，不覆盖、不回退与当前任务无关的内容。
- 阅读根目录 `README.md`、相关模块文档，以及与修改范围直接相关的配置文件。
- 涉及数据库、登录、权限、租户、岗位成员、HC 审批、Agent Run、SSE、Trace 或产物版本时，必须同时检查前端、API、领域状态、存储层、迁移和测试。
- 涉及 Railway 时，先确认本地代码版本、Railway 当前部署版本、服务拓扑和环境变量引用关系一致。
- 查询线上数据时默认只读；不得输出或提交 `DATABASE_URL`、密码、Cookie、Token、API Key 等敏感信息。

## 跨模块影响检查

每次新增或修改功能后，必须明确检查以下问题：

- 前端页面、状态管理、API Client 与后端路由是否使用同一字段和状态定义。
- API Schema、领域类型、数据库 Schema、迁移和持久化 JSON 是否兼容。
- 权限、租户隔离、岗位成员关系是否仍然正确。
- 旧数据、历史版本、空状态、错误状态和重复请求是否仍能正常处理。
- 新增或删除的接口、组件、状态字段是否还有其他调用方。
- Agent Run、SSE、异步任务、Trace 和审计记录是否受到影响。
- 画像产物、HC 审批数据、版本号、状态转换和下游失效逻辑是否保持一致。
- 是否需要新增或更新单元测试、集成测试、迁移验证和线上 Smoke Test。

不能只验证修改的页面；必须检查被共享状态、接口或数据库结构影响的相邻模块。

## 提交前 Review

提交前必须基于实际 diff 进行 Review，至少检查：

- 功能正确性和边界条件。
- 是否引入跨模块回归。
- 权限、租户隔离和敏感数据泄露风险。
- 数据库迁移是否可重复、可回滚，是否兼容现有 Railway 数据。
- 异步流程、并发、缓存、SSE 和错误处理。
- 性能、可维护性和重复逻辑。
- 测试是否覆盖新增逻辑及主要失败路径。

Review 必须先输出发现的问题和修复建议。发现 P0/P1、阻塞合入问题或未解决的跨模块冲突时，不得提交或部署，必须先修复并重新 Review。

建议至少执行：

```bash
git diff --check
git diff
corepack pnpm typecheck
corepack pnpm test
corepack pnpm build
```

如果环境缺少 Node、Corepack、pnpm、数据库或其他依赖，必须明确记录未执行的检查及原因，不得把未验证的结果称为通过。

## GitHub 提交流程

Review 和验证通过后：

1. 再次检查 `git status` 和 `git diff`，确认没有混入无关文件、密钥、构建产物或临时文件。
2. 使用清晰的提交信息提交本次修改。
3. 推送到当前 GitHub 远程仓库和目标分支。
4. 记录提交 hash，并确认远程分支已包含该提交。

不要使用 `git reset --hard`、强制推送或覆盖远程历史，除非用户明确授权。

## Railway 部署流程

GitHub 提交完成后，使用项目已有的 Railway 配置部署，不要自行新建服务或修改生产数据：

1. 确认部署使用的提交 hash 与刚刚提交的版本一致。
2. 确认 `web`、`api`、`harness-sidecar` 和 PostgreSQL 的服务关系没有被破坏。
3. 确认数据库迁移只包含本次必要变更，并先做线上数据备份或只读核验。
4. 执行 Railway 部署或触发对应服务的部署。
5. 检查公开 Web 的 `/healthz`、API 健康状态、登录、岗位列表、岗位详情和岗位画像关键链路。
6. 涉及 Agent、SSE、Trace、HC 或权限时，至少验证一个经理账号、一个 HR 账号和一个管理员账号的相关权限边界。
7. 记录 Railway 部署结果、服务状态和验证结果。

禁止在没有备份、迁移检查或回滚方案的情况下直接修改生产数据库。迁移执行前必须确认不会删除或覆盖现有岗位、HC、对话、画像产物和审计数据。

## 交付报告

完成后必须说明：

- 修改了哪些文件和功能。
- Review 发现了哪些问题，以及如何处理。
- 执行了哪些测试、构建和线上验证。
- GitHub 提交 hash 和推送结果。
- Railway 部署版本、服务状态和关键链路验证结果。
- 仍然存在的风险、未执行的检查或需要用户确认的事项。
