# 岗位画像澄清 Agent PRD 反哺清单 v0

## PRD 反哺清单(prd-to-frontend -> prd-writer)

| # | 类型 | 发现 | 涉及 Sample | 建议处理 |
|---|---|---|---|---|
| F-01 | 信息架构调整 | “岗位项目”和“对话 Session”必须在导航层明确分开；左栏应展示岗位项目与阶段，而不是最后一条聊天消息 | S-01,S-04 | 在 PRD 的项目连续性和前端范围中补充：岗位项目是一级导航对象，一个项目可关联多个 Session |
| F-02 | 流程缺口 | 用人经理可先创建项目并开始澄清，HR 不应成为项目创建的前置条件；HR 后续加入并补充招聘约束 | S-01 | 将核心流程写成“经理创建→Agent获取背景→HR加入补充约束”，并定义 HR 未加入状态下可继续与不可继续的动作 |
| F-03 | 状态补充 | 正式画像字段需要面向用户显示 `系统获取/Agent推断/待确认/已确认/冲突/失效` 等事实状态 | S-01,S-03 | 将事实状态增加为前端展示与验收要求；关键冲突时对应字段标记并阻断发布 |
| F-04 | 边界条件 | 画像一方确认后若关键字段再次变化，其确认是否自动失效尚未正式定义 | S-03,S-04 | `[NEEDS CLARIFICATION]`：建议关键字段变更使相应角色确认失效，并记录失效字段和原因 |
| F-05 | 文案假设 | 工作台当前使用“岗位画像完成度 46%”，但完成度的计算口径尚无产品定义 | S-01 | `[NEEDS CLARIFICATION]`：定义完成度是字段完整率、确认率还是关键链路完整率；正式定义前不得用于绩效比较 |
| F-06 | 状态补充 | 校准阶段需要同时展示“当前 V0、候选人证据矩阵、建议变更及影响”，不能只在对话中给出文字建议 | S-04 | 在校准建议产物中增加 before/after、触发证据、影响、置信/样本提示和审批状态 |
| F-07 | 边界条件 | 移动端无法有效承载完整的批量简历证据矩阵 | S-04 | 明确移动端仅负责快速回答、查看和确认；批量矩阵默认桌面端完成，移动端展示摘要与异常 |
| F-08 | 状态补充 | “感觉不合适”应成为待澄清招聘反馈，不应直接形成新的 Must-have | S-05 | 在招聘反馈对象中增加 `raw/needs-evidence/structured/compliance-review/confirmed` 状态 |
| F-09 | 流程缺口 | S-03 存在关键冲突时，即使双方形式确认也不应允许发布 | S-03 | 将发布门禁明确为“双角色确认＋无关键冲突＋Must-have 可追溯”，并使用程序规则校验 |
| F-10 | 指标变化 | 原型验证暴露出“当前任务是否一眼可见”和“从证据定位原始对话”两个关键可用性指标 | S-01,S-03 | 增加任务定位成功率、证据定位成功率或对应可用性测试，不建议在无真实基线时先设数值目标 |
| F-11 | 边界条件 | 双人确认不能通过同一登录角色模拟完成；HR 未加入时只允许邀请或提醒 | S-03,S-04 | 在权限验收中增加“经理不可代替 HR 确认”；原型采用显式身份切换验证，生产环境必须读取真实登录身份和项目确认记录 |

## 本轮前端验证结论

- S-01：模糊需求、信息缺失和逐步追问已映射到对话与画像草稿。
- S-02：代理条件保持原要求，并通过业务风险追问形成行为化改写预览。
- S-03：冲突来源对照、画像字段标红和发布阻断已实现。
- S-04：证据矩阵区分支持、可能支持、未提及、不符与待问，未使用单一匹配分。
- S-05：非结构化反馈不会写入人才规格，并提供 HR 合规复核路径。
